# 默认行为：自动处理行尾
* text=auto

# 强制这些脚本文件必须保留 LF (这对 mvnw 很重要)
*.sh text eol=lf
mvnw text eol=lf

# Windows 批处理文件强制 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Explicitly declare text files you want to always be normalized and converted
*.js text
*.jsx text
*.ts text
*.tsx text
*.json text
*.md text
*.css text
*.html text
*.yml text
*.yaml text

# Denote all files that are truly binary and should not be modified
*.png binary
*.jpg binary
*.ico binary